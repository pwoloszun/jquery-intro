/*
* When user opens page
* 1) el with class="box" should smoothly fade in (1200ms)
* 2) When user clicks on el with class="box"
* then el with class "square" should smoothly move to that
* clicked position (900ms), and log on console message "Animation finished"
* at the end of animation
* */
function moveOnClick() {
}

$(document).on("ready", function() {
});
