/*
* Attach following events to all <a> elements within element with id="categories":
* 1) on click: log on console value of href attribute of clicked element
* 2) on dblclick: append clicked link txt with "!!!"
* 3) on mouseover: highlight link
* 4) on mouseout: remove highlight
* */
function attachEvents() {
}

/*
* When user double clicks on h5 el within el with id="categories"
* then click event should be triggered for all <a> elements
* */
function triggerEvents() {
}

$(document).on("ready", function() {
  //someDomFunction();
});
